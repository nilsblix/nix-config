source <(fzf --zsh)

export STARSHIP_CONFIG=~/.config/starship/starship.toml
eval "$(starship init zsh)"

# export PATH=$PATH:~/.local/share/zig-macos-aarch64-0.14.0
# export PATH=$PATH:~/.local/share/zig-macos-aarch64-0.13.0

alias proj="cd ~/personal"
alias vim="nvim"

alias l="eza --long --all"
alias ls="eza"
alias la="eza --all"
alias ll="eza --long"

alias hmsi="home-manager switch --impure --flake"
alias drsi="darwin-rebuild switch --impure --flake"
