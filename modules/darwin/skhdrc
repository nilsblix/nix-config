:: default
:: leader
:: open
:: fbrowsers

cmd - a : echo Hello

# default, leader, open, fbrowsers < escape ; default
# f12 ; leader
# 
# leader < o : echo Oopen
# leader < f ; fbrowsers
# leader < e : open -a Alacritty ; default
# 
# open < m : open -a Messages ; default
# open < s : open -a Spotify ; default
# 
# fbrowsers < s : open -a Safari ; default
# fbrowsers < g : open -a 'Google Chrome' ; default

